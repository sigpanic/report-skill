---
name: "算法设计实验报告生成器"
description: "Generate '实验报告' Word documents following template format strictly. Invoke when user wants to create this type of report."
---

# 算法设计实验报告生成器

生成「实验报告」Word文档，严格遵循模板格式。
Profile JSON已就绪，本Skill指导你完成报告生成。

调用本Skill涉及的MCP工具时，需要传入凭据以确认你已阅读本文件。凭据分为两部分，本文件中包含前半部分：**RPT-算法设计实验报告生成器-2026-**，后半部分在文件末尾。调用工具时请将两部分拼接后传入skill_key参数。

## 🚫 关键约束（必须遵守，违反任何约束都是不可接受的）

⚠️ 以下为全局约束，适用于整篇报告。各章节的特定约束见下方「章节结构」区域，此处不重复列出。

🚫 **[禁止内容]** 源代码: 报告中不需要列程序源代码，只需要把代码打包发送
🚫 **[格式要求]** 字体：宋体小四；正文行间距：固定值22磅；每一段首行缩进2个字符；段前及段后间距均设为0

**以上约束来自模板原文，生成报告时必须严格遵守。不要以任何理由违反这些约束。**


## 模板信息
- 模板文件: `算法设计与分析实验报告模板-最新_1.docx`
- 纸张大小: 21.0cm x 29.7cm
- 页边距: 左3.0cm 右3.0cm 上2.54cm 下2.54cm

## 自动删除规则

- 包含「（注：」的段落将被自动删除（子串匹配）
- 包含「(注：」的段落将被自动删除（子串匹配）
- 包含「注：」的段落将被自动删除（子串匹配）
- 包含「（写实验报告时，删除此注释）」的段落将被自动删除（子串匹配）
- 包含「(写实验报告时，删除此注释)」的段落将被自动删除（子串匹配）
- 匹配「根据封皮题目写上本次实验名称」的段落将被自动删除（正则匹配）
- 匹配「年   月   日」的段落将被自动删除（正则匹配）


## 字段定义

### 封面页字段

| 字段Key | 标签 | 类型 | 默认值 |
|---------|------|------|--------|
| `course_name` | 实验课程 | text | 算法设计与分析 |
| `student_name` | 学生姓名 | text |  |
| `student_id` | 学    号 | text |  |
| `student_class` | 专业班级 | text |  |

### 表格字段

| 字段Key | 标签 | 单元格 | 类型 |
|---------|------|--------|------|
| `experiment_name` | 实验名称 | 0,1 | table_cell |
| `experiment_date` | 实验日期 | 1,1 | table_cell |
| `experiment_location` | 实验地点 | 1,3 | table_cell |


## 章节结构

- **一、实验目的** (标题样式: 黑体, 14.0pt, 加粗)
  - 🚫 约束[min_count]: 不少于4个实验目的 (值: 4)
  - 🚫 约束[content]: 内容包括：掌握算法基本原理、掌握算法设计方法、掌握递归函数设计（如用到）、掌握求解问题具体步骤、具备运用算法解决实际问题的能力
- **二、实验环境** (标题样式: 黑体, 14.0pt, 加粗)
  - 🚫 约束[content]: 说明本次实验使用的平台和相关软件
- **三、实验内容** (标题样式: 黑体, 14.0pt, 加粗)
  - 🚫 约束[content]: 描述本次实验的问题
- **四、算法描述** (标题样式: 黑体, 14.0pt, 加粗)
  - 🚫 约束[content]: 用伪代码、流程图或自然语言描述算法的求解步骤
- **五、实验结果** (标题样式: 黑体, 14.0pt, 加粗)
  - 🚫 约束[content]: 给出实验运行过程及结果的界面截图
  - 🚫 约束[content]: 对算法的执行过程进行跟踪分析
  - 🚫 约束[content]: 分析算法的执行效率和复杂性
  - 🚫 约束[content]: 给出详细的文字描述
- **六、实验总结** (标题样式: 黑体, 14.0pt, 加粗)
  - 🚫 约束[content]: 总结实验遇到的问题或难题
  - 🚫 约束[content]: 说明难题如何解决
  - 🚫 约束[content]: 描述实验的体会、启发、收获
  - 🚫 约束[content]: 内容要详细，不能仅几句话
- **一、实验目的** (标题样式: 黑体, 14.0pt, 加粗)
- **二、实验环境** (标题样式: 黑体, 14.0pt, 加粗)
- **三、实验内容** (标题样式: 黑体, 14.0pt, 加粗)
- **四、算法描述** (标题样式: 黑体, 14.0pt, 加粗)
- **五、实验结果** (标题样式: 黑体, 14.0pt, 加粗)
- **六、实验总结** (标题样式: 黑体, 14.0pt, 加粗)


## 格式规则

- 正文: 字体=宋体, 字号=12.0pt
- 章节标题: 字体=黑体, 字号=14.0pt, 加粗
- 行间距: 固定值22.0磅
- 首行缩进: 2个字符
- 段前段后间距: （未指定，由模板决定）

## 约束规则

### 禁止内容
- 源代码: 报告中不需要列程序源代码，只需要把代码打包发送

### 格式要求
字体：宋体小四；正文行间距：固定值22磅；每一段首行缩进2个字符；段前及段后间距均设为0


## 工作流程

### 步骤1：创建输出目录
- 在类别目录下创建本次报告的子目录
- 生成的报告和相关文件输出到该子目录

### 步骤2：读取课件（如有）
- 如果用户提供了课件文件，调用`parse_course_material`解析课件
- 解析结果默认保存在课件所在目录
- 根据课件内容理解报告要求
- 如果没有课件，根据用户描述理解要求

### 步骤3：准备内容
- 根据课件/资料内容，准备各章节的文字内容
- 章节列表：
{{section_list}}
- 每个章节的内容必须遵守上方"各章节要求"中的说明
- 内容风格应自然，避免AI感

### 步骤4：准备字段值
- 收集所有需要填写的字段：
{{field_list}}
- 个人信息（姓名、学号、班级）由系统自动从.env填充，无需询问用户
- 其他字段优先使用Profile中的默认值，不确定的询问用户

### 步骤5：调用MCP工具生成文档
- 调用 generate_report 工具，传入：
  - template_path: 模板文件路径
  - output_path: 输出路径（后缀由工具自动处理）
  - profile_path: TemplateProfile JSON路径
  - field_values: 字段值字典
  - sections: 章节内容数组
- sections数组中每项的title必须与Profile中的section title完全匹配

### 步骤6：验证输出
- 调用 verify_format 工具验证格式一致性
- 检查所有字段是否正确填充
- 如果格式不一致，分析原因并修正

## MCP工具

### parse_course_material
解析课件文件（pptx/ppt/docx/doc），提取文本内容。
- file_path（必需）: 课件文件路径
- output_path（可选）: 解析结果保存路径（默认保存在课件所在目录）

### generate_report
生成报告Word文档（输出格式自动与模板一致）。
- template_path（必需）: 模板文件路径
- output_path（必需）: 输出文件路径
- profile_path（必需）: TemplateProfile JSON文件路径
- field_values（必需）: 字段值字典
- sections（必需）: 章节内容数组，每项含title和content
- result_images（可选）: 结果截图路径列表

### verify_format
对比生成的Word文档与模板的格式差异。
- template_path（必需）: 模板文件路径
- generated_path（必需）: 生成的文档路径

## 关键注意事项
- 生成的Word文档格式必须与模板完全一致——最高优先级
- 所有注释段落会被自动删除（根据annotation_patterns/removal_patterns）
- 章节内容按Profile中的格式规则排版
- 如果有任何不确定的信息，直接询问用户，不要自己编造
- 模板中的注释和说明必须遵守
- 输出文件后缀由工具自动保证（.doc模板→.doc输出，.docx→.docx）
- **🚫 你必须遵守上方「关键约束」区域的所有约束（如有），违反任何约束都是不可接受的**

凭据后半部分：**48568613**。请与前半部分拼接后传入skill_key参数。
